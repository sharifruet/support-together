version: '3.1'

services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: "support"
      MYSQL_USER: "support"
      MYSQL_PASSWORD: "support"
      MYSQL_ROOT_PASSWORD: "toor"
    ports:
      - 3306:3306
      